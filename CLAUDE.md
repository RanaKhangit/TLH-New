## Tool Restrictions

DO NOT use Bash for file operations. Always use dedicated tools:
- Reading files → Use `Read` tool
- Editing files → Use `Edit` tool
- Creating files → Use `Write` tool
- Finding files → Use `Glob` tool
- Searching content → Use `Grep` tool

Bash is ONLY allowed for: git, npm, docker, and starting services.
