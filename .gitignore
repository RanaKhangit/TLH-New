node_modules

# Environment files (secrets)
.env
.env.*
!.env.example

# Key material
*.key
*.pem

# Chainlink Node secrets (already in chainlink-node/.gitignore but belt-and-suspenders)
secrets.toml
api.txt

# OS files
.DS_Store
Thumbs.db

# Foundry
contracts/out/
contracts/cache/

# Build outputs
**/dist/

# Local binaries
foundry_bin/

# Windows artifacts
nul

# Client PDFs (large binaries — keep .md analysis only)
Project info/*.pdf

# Dry-run broadcast artifacts
contracts/broadcast/**/dry-run/

# Private chain validator data (generated, local)
private-chain/data/
